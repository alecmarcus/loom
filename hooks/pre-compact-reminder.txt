CONTEXT COMPACTION — Re-read your directives after compaction.

After this compaction completes, you MUST:
1. Re-read templates/orchestrator.md (your full protocol)
2. Re-read any in-progress issue bodies
3. Check issue comments to determine where you left off in the loop
4. Resume from the correct step — do NOT restart or skip the review cycle

The review cycle is: coder → reviewer → arbiter → (if findings > 0) → coder → repeat.
Do NOT skip directly to merging or shipping.